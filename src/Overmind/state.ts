import eCard from "../Entities/eCard"

type State = {
  cards: eCard[]
  // foo: string
  // bar: boolean
  // baz: string
  // user: User
}

export const state: State = {
  cards: []
  // foo: 'bar',
  // bar: true,
  // baz: [],
  // user: new User()
}